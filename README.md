# plp-python-week3 - Grade Reporter & Bug Hunt

grade_reporter.py - Loops through scores, assigns A/B/C/F grades, counts pass/fail and calculates average.
bug_hunt.py - Fixed broken while-loop that sums 1 to 5 with 3 bug explanations.

The hardest bug was the logic bug where count < 5 instead of <=5. It was hardest because there was no error message, the program ran and printed 10 instead of 15. I knew something was wrong only because the output was wrong, not because Python showed an error.